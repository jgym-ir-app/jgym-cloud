{
  "name": "jgym-cloud-server",
  "version": "1.0.0",
  "description": "J,Gym Cloud Server - Express + Socket.io",
  "main": "cloud-server.js",
  "scripts": {
    "start": "node cloud-server.js",
    "build": "esbuild cloud-server.ts --bundle --platform=node --format=cjs --packages=external --outfile=cloud-server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.0",
    "mongodb": "^6.12.0",
    "socket.io": "^4.8.0"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
